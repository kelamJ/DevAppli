<aside class="col-3 border border-light bg-primary p-2">
	<article class="card mb-3 bg-primary text-white ">
		<div class="card-header border-light ">
			Catégories
		</div>
		<div class="card-body">
			<p >
				<a class="text-white" href="{{ path('admin_categories_index') }}">Liste des catégories</a>
			</p>
			<p>
				<a class="text-white" href="{{ path('admin_categories_add') }}">Ajouter une catégorie</a>
			</p>
		</div>
	</article>
	<article class="card mb-3 bg-primary text-white">
		<div class="card-header border-light">
			Plats
		</div>
		<div class="card-body">
			<p>
				<a class="text-white" href="{{ path('admin_plats_index') }}">Liste des plats</a>
			</p>
			<p>
				<a class="text-white" href="{{ path('admin_plats_add') }}">Ajouter un plat</a>
			</p>
		</div>
	</article>
	<article class="card mb-3 bg-primary text-white">
		<div class="card-header border-light">
			Utilisateurs
		</div>
		<div class="card-body">
			<p>
				<a class="text-white" href="{{ path('admin_users_index') }}">Liste des utilisateurs</a>
			</p>
			<p>
				<a class="text-white" href="{{ path('admin_users_gestion') }}">Gestion des info utilisateurs</a>
			</p>
		</div>
	</article>
	
</aside>
