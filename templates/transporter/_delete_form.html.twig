<form method="post" action="{{ path('admin_trans_delete', {'id': transporter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transporter.id) }}">
    <button class="btn">Delete</button>
</form>
