{% extends 'base.html.twig' %}

{% block title %}Liste des catégories
{% endblock %}

{% block body %}
	{# <div class="container">
		<img class="img-fluid rounded mx-auto d-block" src="/assets/uploads/bcg1.png" alt="responsive_bg1" width="33%">

		<form class="d-flex">
			<input class="btn-outline-primary form-control me-sm-2" type="search" placeholder="Search">
			<button class="btn btn-outline-primary btn-secondary my-2 my-sm-0" type="submit">Search</button>
		</form>
	</div> #}

	<main class="container text-white">
		<section class="row">
			<div class="col-12">
				<h1 class="mt-3 text-white">Liste des catégories</h1>
			</div>
			{% for category in categorie %}
				<div class="col-lg-4 mb-3">
					<div class="card h-100 bg-success">
						<img class="card-img-top" src="{{ asset('assets/uploads/categories/') ~ category.image }}" alt="Card image cap">
						<div class="card-body">
							<h3 class="card-title text-danger">{{ category.libelle }}</h3>
							<p class="card-text">Disponible : {{ category.active }}</p>
							<a href="{{ path('categorie_list', {id: category.id})}}" class="btn btn-danger">Voir la catégorie</a>
						</div>
					</div>
				</div>
			{% endfor %}

		</section>
	</main>

{% endblock %}